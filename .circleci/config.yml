version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      build: 
        docker: 
          — image: circleci/node:12.18
        working_directory: ~/feature
        steps:
          — checkout
          
          — run: npm install
          — run: npm test
